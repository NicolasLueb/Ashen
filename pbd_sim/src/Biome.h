#pragma once
// ============================================================
//  Biome — defined here so both Level.h and GameState.h
//  can include it without circular dependency
// ============================================================
enum class Biome { Catacombs = 0, Cathedral = 1, Abyss = 2 };
